DROP TABLE COUNTRIES CASCADE CONSTRAINT 

DROP TABLE DEPARTMENTS  CASCADE CONSTRAINT 

DROP TABLE EMPLOYEES  CASCADE CONSTRAINT 

DROP TABLE LOCATIONS  CASCADE CONSTRAINT 

DROP TABLE REGIONS  CASCADE CONSTRAINT 

DROP TABLE JOBS  CASCADE CONSTRAINT 

CREATE TABLE COUNTRIES AS SELECT * FROM HR.COUNTRIES

CREATE TABLE DEPARTMENTS AS SELECT * FROM HR.DEPARTMENTS

CREATE TABLE EMPLOYEES AS SELECT * FROM HR.EMPLOYEES 

CREATE TABLE JOBS AS SELECT * FROM HR.JOBS

CREATE TABLE JOB_GRADES AS SELECT * FROM HR.JOB_GRADES

CREATE TABLE JOB_HISTORY AS SELECT * FROM HR.JOB_HISTORY

CREATE TABLE LOCATIONS AS SELECT * FROM HR.LOCATIONS

CREATE TABLE PRODUCTS AS SELECT * FROM HR.PRODUCTS

CREATE TABLE REGIONS AS SELECT * FROM HR.REGIONS

CREATE TABLE SALES AS SELECT * FROM HR.SALES

 --  *alter table*
 --  Program sie wyłączył i nie było zapisane scripts

ALTER TABLE SALES 
ADD CONSTRAINT SAL_EMP_ID_FK
FOREIGN KEY (employee_id) REFERENCES EMPLOYEES(employee_id)

ALTER TABLE SALES 
ADD CONSTRAINT SAL_ID_PK
PRIMARY KEY (sale_id)

ALTER TABLE EMPLOYEES 
ADD CONSTRAINT EMP_JOB_ID_FK
FOREIGN KEY (job_id) REFERENCES JOBS(job_id)

ALTER TABLE JOB_HISTORY 
ADD CONSTRAINT EMP_ID_START_DATE_PK
PRIMARY KEY (employee_id, start_date)

ALTER TABLE JOB_HISTORY 
ADD CONSTRAINT JOB_HIST_EMP_ID_FK
FOREIGN KEY (employee_id) REFERENCES EMPLOYEES(employee_id)

ALTER TABLE JOB_HISTORY 
ADD CONSTRAINT JOB_HIST_DEP_ID_FK
FOREIGN KEY (department_id) REFERENCES DEPARTMENTS(department_id)

ALTER TABLE JOB_HISTORY 
ADD CONSTRAINT JOB_HIST_JOB_ID_FK
FOREIGN KEY (job_id) REFERENCES JOBS(job_id)

ALTER TABLE JOB_GRADES 
ADD CONSTRAINT JOB_GRAD_ID_PK
PRIMARY KEY (grade)

===== zad 1 ===== 

SELECT LAST_NAME||' '||SALARY AS wynagrodzenie  
FROM EMPLOYEES 
WHERE 1=1
		AND DEPARTMENT_ID IN (20,50)
		AND SALARY BETWEEN 2000 AND 7000
		ORDER BY LAST_NAME 

===== zad 2 =====

SELECT HIRE_DATE ,LAST_NAME, ${col}
FROM EMPLOYEES
WHERE 1=1
	AND MANAGER_ID IS NOT NULL 
	AND EXTRACT(YEAR FROM HIRE_DATE) = 2005
	ORDER BY ${col} 

===== zad 3 =====

SELECT FIRST_NAME|| ' ' || LAST_NAME AS imie_nazwisko, SALARY, PHONE_NUMBER 
FROM EMPLOYEES
	WHERE 1=1
	AND substr(LAST_NAME, 3,1) = 'e'
	AND LOWER(FIRST_NAME ) LIKE '%'||'${name}'||'%'
	ORDER BY 1 DESC, 2 ASC

===== zad 4 =====
SELECT
    FIRST_NAME,
    LAST_NAME,
    ROUND(MONTHS_BETWEEN(SYSDATE, HIRE_DATE)) AS MONTHS_WORKED,
    CASE 
        WHEN MONTHS_BETWEEN(SYSDATE, HIRE_DATE) >= 200 THEN SALARY * 0.30
        WHEN MONTHS_BETWEEN(SYSDATE, HIRE_DATE) >= 150 THEN SALARY * 0.20
        ELSE SALARY * 0.10
    END AS WYSOKOSC_DODATKU
FROM EMPLOYEES
ORDER BY MONTHS_WORKED ASC;

============= zad 5 ===========
SELECT
    DEPARTMENT_ID, 
    SUM(SALARY) AS SUMA_ZAROBKOW, 
    ROUND(AVG(SALARY)) AS SREDNIA_ZAROBKOW
FROM EMPLOYEES
GROUP BY DEPARTMENT_ID
HAVING MIN(SALARY) > 5000;

============= zad 6 ===========

SELECT 
    e.LAST_NAME, 
    e.DEPARTMENT_ID, 
    d.DEPARTMENT_NAME, 
    e.JOB_ID
FROM EMPLOYEES e
JOIN DEPARTMENTS d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID
JOIN LOCATIONS l ON d.LOCATION_ID = l.LOCATION_ID
WHERE l.CITY = 'Toronto';
============= zad 7 ===========
SELECT 
    e1.FIRST_NAME AS PRACOWNIK, 
    e1.LAST_NAME AS NAZWISKO_PRAC,
    e2.FIRST_NAME AS WSPOLPRACOWNIK, 
    e2.LAST_NAME AS NAZWISKO_WSPOL
FROM EMPLOYEES e1
JOIN EMPLOYEES e2 ON e1.DEPARTMENT_ID = e2.DEPARTMENT_ID
WHERE e1.FIRST_NAME = 'Jennifer'
AND e1.EMPLOYEE_ID <> e2.EMPLOYEE_ID;

============= zad 8 ===========

SELECT DEPARTMENT_NAME
FROM DEPARTMENTS
WHERE DEPARTMENT_ID NOT IN (
    SELECT DISTINCT DEPARTMENT_ID 
    FROM EMPLOYEES 
    WHERE DEPARTMENT_ID IS NOT NULL
);
============= zad 9 ===========
SELECT 
    e.FIRST_NAME, 
    e.LAST_NAME, 
    e.JOB_ID, 
    d.DEPARTMENT_NAME, 
    e.SALARY, 
    g.GRADE
FROM EMPLOYEES e
LEFT JOIN DEPARTMENTS d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID
JOIN JOB_GRADES g ON e.SALARY BETWEEN g.MIN_SALARY AND g.MAX_SALARY;

============= zad 10 ===========
SELECT FIRST_NAME, LAST_NAME, SALARY
FROM EMPLOYEES
WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES)
ORDER BY SALARY DESC;

============= zad 11 ===========

SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME
FROM EMPLOYEES
WHERE DEPARTMENT_ID IN (
    SELECT DISTINCT DEPARTMENT_ID 
    FROM EMPLOYEES 
    WHERE LAST_NAME LIKE '%u%'
);
============= zad 12 ===========
SELECT FIRST_NAME, LAST_NAME, HIRE_DATE
FROM EMPLOYEES
WHERE HIRE_DATE < (
    SELECT TO_DATE(ROUND(AVG(TO_NUMBER(TO_CHAR(HIRE_DATE, 'J')))), 'J')
    FROM EMPLOYEES
);

============= zad 13 ===========

SELECT 
    d.DEPARTMENT_NAME, 
    COUNT(e.EMPLOYEE_ID) AS LICZBA_PRAC, 
    AVG(e.SALARY) AS SREDNIA_PENSJA
FROM DEPARTMENTS d
LEFT JOIN EMPLOYEES e ON d.DEPARTMENT_ID = e.DEPARTMENT_ID
GROUP BY d.DEPARTMENT_NAME
ORDER BY LICZBA_PRAC DESC;
============= zad 14 ===========

SELECT FIRST_NAME, LAST_NAME, SALARY
FROM EMPLOYEES
WHERE SALARY < ALL (
    SELECT e.SALARY 
    FROM EMPLOYEES e 
    JOIN DEPARTMENTS d ON e.DEPARTMENT_ID = d.DEPARTMENT_ID
    WHERE d.DEPARTMENT_NAME = 'IT'
);
============= zad 15 ===========

SELECT DISTINCT d.DEPARTMENT_NAME
FROM DEPARTMENTS d
JOIN EMPLOYEES e ON d.DEPARTMENT_ID = e.DEPARTMENT_ID
WHERE e.SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES);

============= zad 16 ===========
SELECT * FROM (
    SELECT j.JOB_TITLE, AVG(e.SALARY) AS SREDNIA_PENSJA
    FROM JOBS j
    JOIN EMPLOYEES e ON j.JOB_ID = e.JOB_ID
    GROUP BY j.JOB_TITLE
    ORDER BY SREDNIA_PENSJA DESC
)
WHERE ROWNUM <= 5;

============= zad 17 ===========
SELECT 
    r.REGION_NAME, 
    COUNT(DISTINCT c.COUNTRY_ID) AS LICZBA_KRAJOW, 
    COUNT(e.EMPLOYEE_ID) AS LICZBA_PRAC
FROM REGIONS r
LEFT JOIN COUNTRIES c ON r.REGION_ID = c.REGION_ID
LEFT JOIN LOCATIONS l ON c.COUNTRY_ID = l.COUNTRY_ID
LEFT JOIN DEPARTMENTS d ON l.LOCATION_ID = d.LOCATION_ID
LEFT JOIN EMPLOYEES e ON d.DEPARTMENT_ID = e.DEPARTMENT_ID
GROUP BY r.REGION_NAME;

============= zad 18 ===========
SELECT 
    e.FIRST_NAME, 
    e.LAST_NAME, 
    e.SALARY AS PENSJA_PRAC, 
    m.SALARY AS PENSJA_SZEFA
FROM EMPLOYEES e
JOIN EMPLOYEES m ON e.MANAGER_ID = m.EMPLOYEE_ID
WHERE e.SALARY > m.SALARY;

============= zad 19 ===========

SELECT 
    TO_CHAR(HIRE_DATE, 'MM') AS NUMER_MIESIACA, 
    TO_CHAR(HIRE_DATE, 'Month') AS NAZWA_MIESIACA, 
    COUNT(*) AS ILOSC
FROM EMPLOYEES
GROUP BY TO_CHAR(HIRE_DATE, 'MM'), TO_CHAR(HIRE_DATE, 'Month')
ORDER BY NUMER_MIESIACA;
============= zad 20 ===========

SELECT * FROM (
    SELECT d.DEPARTMENT_NAME, AVG(e.SALARY) AS SREDNIA_PENSJA
    FROM DEPARTMENTS d
    JOIN EMPLOYEES e ON d.DEPARTMENT_ID = e.DEPARTMENT_ID
    GROUP BY d.DEPARTMENT_NAME
    ORDER BY SREDNIA_PENSJA DESC
) 
WHERE ROWNUM <= 3;


